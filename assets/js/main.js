// assets/js/main.js
console.log("main.js yüklendi.");

// Genel UI etkileşimleri veya diğer modül başlatmaları buraya eklenebilir.
document.addEventListener('DOMContentLoaded', function() {
  // Örneğin, Bootstrap tooltip'lerini etkinleştirme (eğer kullanırsak)
  // var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
  // var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
  //   return new bootstrap.Tooltip(tooltipTriggerEl)
  // })

  // Menü (hamburger) işlevselliği Bootstrap tarafından hallediliyor.
  // Ek özel UI işlevleri ui.js'den çağrılabilir veya burada yazılabilir.
});